#include <bits/stdc++.h>
using namespace std;
#define int long long
const int MOD = 1e9+7;


// Observations
/*
sum of all ai= s;
ai must have bit set that are in m so that ai&m=ai, so bits bounded

now sum = s;

sum = 13, 1101
m = 3, = 011 == 101 == 111 == 1011 == 1101

2^1 + 2^0 given  make 2^3 + 2^2 + 2^0   --- 

m= 6 , 0110 == 1010


*/


void solve(){
    int s,m;cin>>s>>m;




}

int32_t main(){

#ifdef lav1sh
    freopen("input.txt","r",stdin);
    freopen("output.txt","w",stdout);
#endif

    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    int t; cin >> t;
    while(t--) solve();

}